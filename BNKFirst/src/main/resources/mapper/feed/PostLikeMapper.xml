<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.co.bnkfirst.mapper.feed.PostLikeMapper">

    <select id="exists" resultType="int">
        SELECT CASE WHEN COUNT(*) > 0 THEN 1 ELSE 0 END
        FROM POSTLIKE
        WHERE POSTID = #{postId}
          AND "uId" = #{uId}
    </select>

    <insert id="insert">
        INSERT INTO POSTLIKE (POSTID, "uId")
        VALUES (#{postId}, #{uId})
    </insert>

    <delete id="delete">
        DELETE FROM POSTLIKE
        WHERE POSTID = #{postId}
          AND "uId" = #{uId}
    </delete>

    <select id="countByPostId" resultType="long">
        SELECT COUNT(*)
        FROM POSTLIKE
        WHERE POSTID = #{postId}
    </select>

</mapper>

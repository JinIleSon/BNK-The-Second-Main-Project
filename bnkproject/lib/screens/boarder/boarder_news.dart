import 'package:flutter/material.dart';

class BoarderNews extends StatelessWidget {
  const BoarderNews({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('뉴스 피드 (하드코딩 상태)'),
    );
  }
}

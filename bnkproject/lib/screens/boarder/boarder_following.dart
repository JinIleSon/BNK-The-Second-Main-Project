import 'package:flutter/material.dart';

class BoarderFollowing extends StatelessWidget {
  const BoarderFollowing({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('팔로잉 피드 (하드코딩 상태)'),
    );
  }
}

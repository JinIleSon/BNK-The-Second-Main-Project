import 'package:flutter/material.dart';

class BoarderContent extends StatelessWidget {
  const BoarderContent({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('콘텐츠 피드 (하드코딩 상태)'),
    );
  }
}
